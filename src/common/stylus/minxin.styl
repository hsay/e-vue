border-1px($color)
  position: relative
  &::after
    position: absolute
    width: 100%
    left: 0
    bottom: 0
    border-top: 1px solid $color
    content: ' '

bg-image($url)
  background-image: url($url + "@2x.png")
  @media (-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3)
    background-image: url($url + "@3x.png")
/*这里有一个疑惑 minxin.styl与base.styl的关系，minxin主要是定义函数，函数的目的就是通过参数传递的方式使用
  base.styl中是一些公用的属性，比如border-1px()只是为了得到颜色不一样的1px边框，base.styl中的.border-1px
  主要是根据当前平台，对该1px做缩放
*/
